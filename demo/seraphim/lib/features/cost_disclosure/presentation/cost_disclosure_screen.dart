import 'package:flutter/material.dart';
import 'package:solidui/solidui.dart';

class CostDisclosureScreen extends StatelessWidget {
  const CostDisclosureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SolidScaffold(
      body: Center(child: Text('Selective Cost Disclosure')),
    );
  }
}
